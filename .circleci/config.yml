version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.11
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: 'npm install'
      - run:
          name: Run Linter
          command: 'npm run lint'
      - run:
          name: Run Unit Tests
          command: 'npm test'
      - run:
          name: Build Application
          command: 'npm run build'
      - deploy:
          name: Run E2E tests on release ready code
          command: |
            if [ "${CIRCLE_BRANCH}" == "feature/DR-15-ci-cd-setup" ]; then
              npm run e2e
            fi
      - deploy:
          name: Deploy DailyRoutine to GitHub Pages
          command: |
            if [ "${CIRCLE_BRANCH}" == "feature/DR-15-ci-cd-setup" ]; then
              git config --global user.email "marcin.golawski@wipro.com"
              git config --global user.name "BuildIt-Poland"
              npm run deploy
            fi
