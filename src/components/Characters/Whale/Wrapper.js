import styled from 'styled-components';

import { spacingLarge } from '../../../styles/designTokens';

const Wrapper = styled.div`
  padding: 8rem 0 ${spacingLarge} 0;
  position: relative;
`;

export default Wrapper;
