// Allows all animated elements to be positioned correctly

import styled from 'styled-components';

const Wrapper = styled.div`
  position: relative;
  width: 100%;
  max-width: 34rem;
`;

export default Wrapper;
