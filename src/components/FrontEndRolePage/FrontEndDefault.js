import React from 'react';

import { DEFAULT } from '../../constants/roleActions';
import { Fox } from '../Characters';

function FrontEndDefault() {
  return <Fox pose={DEFAULT} />;
}

export default FrontEndDefault;
