import React from 'react';

import { DEFAULT } from '../../constants/roleActions';
import { Robot } from '../Characters';

function BackEndDefault() {
  return <Robot pose={DEFAULT} />;
}

export default BackEndDefault;
