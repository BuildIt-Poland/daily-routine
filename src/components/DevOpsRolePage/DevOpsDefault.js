import React from 'react';

import { DEFAULT } from '../../constants/roleActions';
import { Whale } from '../Characters';

function DevOpsDefault() {
  return <Whale pose={DEFAULT} />;
}

export default DevOpsDefault;
