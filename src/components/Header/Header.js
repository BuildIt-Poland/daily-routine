import React from 'react';

import Layout from './Wrapper';
import Logo from './Logo';

function Header() {
  return (
    <Layout role="banner">
      <Logo />
    </Layout>
  );
}

export default Header;
