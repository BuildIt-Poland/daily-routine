import styled from 'styled-components';

const Wrapper = styled.article`
  display: grid;
  grid-template-rows: auto minmax(43rem, max-content) 1fr;
  align-content: stretch;
  height: 100%;
`;

export default Wrapper;
